#include<iostream>
#include<stdio.h>
using namespace std;
int s;
int d;
int Max=0;
bool mouth[12];
int doit(int now,int all,int sum)
{
    if (now==all)
    {
        if (sum>Max)
            Max=sum;
        return 0;
    }

}
main()
{
    while(cin>>s>>d)
    {

    }
}
